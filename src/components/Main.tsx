import React from 'react';
import Header from './Header';

interface MainProps {}

const Main: React.FC<MainProps> = (props) => {
    return (
        <>
            <Header />
        </>
    );
}

export default Main;